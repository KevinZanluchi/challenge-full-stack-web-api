SQL TABLE:

CREATE TABLE STUDENT(
	ID SERIAL NOT NULL PRIMARY KEY,
	ACADEMICRECORD VARCHAR(12) NOT NULL,
	NAME VARCHAR(20) NOT NULL,
	EMAIL VARCHAR(20) NOT NULL,
	CPF VARCHAR(11) NOT NULL,
	ISDELETE BOOLEAN NOT NULL DEFAULT FALSE
)
